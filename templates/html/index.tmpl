<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8">
  <link href="/css/product_card.css" rel="stylesheet" type="text/css">
</head>
<body>
  <h1>Список получателей</h1>
<p>
<form action="/recipients" method="get">
    <input type="text" name="Name" value = "{{.Name}}" placeholder="Введите ФИО">
    <button type="submit">Фильтровать</button>
</p>
</form>

<section class="articles">
{{range .Recipients}}
  <article>
    <div class="article-wrapper">
      
        <img src={{.ImageSrc}} alt="" />
      
      <div class="article-body">
        <h2>{{.Name.First_name}} {{.Name.Second_name}}</h2>
        <p>
          {{.Email}}
        </p>
        <a href="/recipients/{{.Id}}" class="read-more">
          Подробнее <span class="sr-only">about this is some title</span>
          <svg xmlns="http://www.w3.org/2000/svg" class="icon" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M12.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
          </svg>
        </a>
      </div>
    </div>
  </article>
  {{end}}
</section>
</body>
</html>