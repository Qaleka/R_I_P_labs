<!DOCTYPE html>
<html>
<head>
    <title>Список услуг</title>
    <link href="/css/product_card.css" rel="stylesheet" type="text/css">
</head>
<body>
    <!-- Фильтр -->
    <form action="/recipients" method="get">
    <input type="text" name="filter" value = "{{.filter}}" placeholder="Фильтр">
    <select name = "field">
    <option value = "First_name"> По имени </option>
    <option value = "Email"> По почте </option>
    </select>
    <button type="submit">Фильтровать</button>
    </form>
    <!-- Список услуг в виде карточек -->
    <div class = "cards">
    {{range .Recipients}}
        <div class="card">
            <h2>{{.Name.First_name}}</h2>
            <p>
            <div class = "card_title">
            Email: {{.Email}}
            </div>
            </p>
            <p>Возраст: {{.Age}}</p>
            <img class="card-img-top" src={{.ImageSrc}} alt="Card image cap">
            <a href="/recipients/{{.Id}}" class = "card_add">Подробнее</a>
        </div>
    {{end}}
    </div>
</body>
</html>