<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8">
  <link href="/css/product_card.css" rel="stylesheet" type="text/css">
</head>
<body>
  <h1>Список получателей</h1>
<p>
<form action="/recipients" method="get">
    <input type="text" name="Name" value="{{if ne .Name ""}}{{.Name}}{{end}}" placeholder="Введите ФИО">
    <button type="submit" class = "button-7">Фильтровать</button>
</p>
</form>

<section class="articles">
{{range $key, $value := .Recipients}}
  <article>
    <div class="article-wrapper">
      
        <img src={{$value.ImageURL}} alt="" />
      
      <div class="article-body">
        <h2>{{$value.FIO}}</h2>
        <p>
          {{$value.Email}}
        </p>
        {{if not .Delivered}}
        <form action="/recipients" method="post">
                        <input type="hidden" name="deliver" value="{{.RecipientId}}">
                        <input type="submit" value="Доставить" class="read-more" style="width: 100%;">
                    </form>
        {{end}}
        <a href="/recipients/{{$value.RecipientId}}" class="read-more">
          Подробнее <span class="sr-only">about this is some title</span>
          <svg xmlns="http://www.w3.org/2000/svg" class="icon" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M12.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
          </svg>
        </a>
      </div>
    </div>
  </article>
  {{end}}
</section>
</body>
</html>